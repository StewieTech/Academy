import React from 'react';
        import { render } from '@testing-library/react';
        import DisplayComponent from './DisplayComponent';
        
        test('renders DisplayComponent component', () => {
            render(<DisplayComponent />);
        });